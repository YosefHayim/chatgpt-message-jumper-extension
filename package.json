{
  "name": "ai-conversation-navigator",
  "displayName": "AI Conversation Navigator",
  "version": "2.0.0",
  "description": "Advanced navigation and analytics for ChatGPT, Claude, and Gemini conversations",
  "author": "YosefHayim",
  "license": "MIT",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc && npm run copy-assets",
    "copy-assets": "mkdir -p dist && cp -r icons dist/ && cp assets/manifest.json assets/popup.html assets/popup.css assets/styles.css dist/",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@testing-library/jest-dom": "^6.1.5",
    "@types/chrome": "^0.0.258",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.6",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}
